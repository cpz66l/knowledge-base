# GameDev 知识库智能体规则

## 适用范围

本文件适用于整个仓库。仓库用于沉淀已经学习、理解或实际验证过的游戏客户端开发知识，正式内容位于 `docs/`，通过 MkDocs Material 发布。

## 跨模型交接

- 不同模型或智能体接手仓库时，必须同时阅读根目录的 `HANDOFF.md`。
- `AGENTS.md` 定义不可违背的仓库规则，`HANDOFF.md` 定义接手检查、证据归属、脏工作区保护和未完成任务交接格式。
- 两者冲突时以 `AGENTS.md` 为准；任何旧聊天总结或处理报告都必须通过当前文件和工具结果复核。

## 核心原则

- `inbox/` 是零整理成本的知识收件箱。用户可以直接放入散乱的笔记、代码、截图、PDF、链接文件或整个脚本目录。
- 不要求用户预先分类、重命名、建立子目录或填写元数据。
- 智能体负责识别主题、去重、提炼、验证、写入知识库和归档。
- 不把“收到的资料”直接视为正确结论。必须区分已验证事实、个人理解、外部资料观点和待验证内容。
- 不为了让文章看起来完整而补写用户尚未学习或无法验证的结论。
- 原始资料不得直接删除或覆盖。

## 目录职责

- `inbox/`：用户随手投入的原始资料，只处理 `.gitkeep` 之外的内容。
- `archive/`：已经处理的原始资料，由智能体按处理日期自动归档。
- `examples/`：从原始脚本中提炼出的、适合长期保留的代码示例。
- `docs/`：正式知识文章，是知识库的唯一权威版本。
- `docs/guides/note-template.md`：新建知识文章时使用的基本结构。
- `mkdocs.yml`：站点导航。只有新增正式页面时才需要更新。

## Inbox 处理触发条件

只有当用户明确要求“整理 inbox”“处理收件箱”或提出等价请求时，才执行完整入库流程。普通问答或其他代码任务不得擅自移动 `inbox/` 中的内容。

## 标准入库流程

1. 扫描 `inbox/` 中的全部新增文件和目录，包括通常会被 Git 忽略的内容。
2. 按内容识别主题，不依赖用户提前整理。多个文件讨论同一问题时，应作为同一批资料综合理解。
3. 搜索 `docs/` 中的现有文章：
   - 已有对应页面时，优先补充或修正现有页面。
   - 只有形成独立主题且现有页面不适合承载时，才创建新页面。
   - 同一知识点只能保留一个权威页面，避免重复文章。
4. 提炼内容时明确区分：
   - 已运行、已实验或有证据支持的结论；
   - 用户当前的理解和实践经验；
   - 外部资料中的观点；
   - 无法确认、需要继续实验的问题。
5. 正式文章遵循 `docs/guides/note-template.md`，重点保留学习目标、当前理解、最小示例、项目应用、常见错误和验证方法。不适用的章节可以省略，禁止机械填充。
6. 可复用代码才整理到 `examples/`。实验代码、残缺片段或强依赖具体项目的代码，应在文章中节选说明，不能包装成通用最佳实践。
7. 新建正式页面后更新 `mkdocs.yml`，并在相关页面之间添加必要的关联链接。
8. 完成内容修改后执行站点构建验证。默认运行 `mkdocs build`；如果环境缺少依赖，应如实说明，不得声称验证成功。
9. 将已处理的原始资料移动到 `archive/YYYY-MM-DD/`。如遇同名文件，保留两份并添加可区分的后缀，不得覆盖。
10. 在当次归档目录创建 `processing-report.md`，记录：
    - 本次处理了哪些资料；
    - 更新或创建了哪些知识页面；
    - 提取了哪些代码示例；
    - 哪些内容仍待确认；
    - 执行了哪些验证及结果。

## 无法判断时的处理

- 能可靠提炼的部分正常入库，不能确认的部分明确标记为“待验证”。
- 完全缺少上下文且可能导致错误归类时，不猜测、不编造；将原文件留在 `inbox/`，并在处理结果中提出一个简短、具体的问题。
- 截图、二进制文件或损坏文件无法读取时，说明限制并保留原文件。

## Unity 与 C# 资料规则

- Unity 脚本必须尽量注明可推断出的 Unity 版本、依赖包、挂载对象、调用入口和生命周期要求。
- 如果资料包含 Prefab、场景、ScriptableObject 或资源引用，应检查相关 `.meta` 文件和 GUID 关系，不得只根据单个脚本下结论。
- 不扫描或归档 Unity 生成目录中的大量缓存文件，例如 `Library/`、`Temp/`、`Logs/`、`obj/` 和构建产物；发现时跳过并在报告中说明。
- 无法在当前环境运行 Unity 时，只能声明完成了静态检查，不得声称代码已经在 Unity 中运行通过。
- 保留有教学价值的最小代码；项目专用名称、密钥、个人路径和无关业务代码应删除或匿名化。

## LeetCode 双语入库规则

- 通过题号识别并配对同一道题的 C# 与 C++ 内容；文件名不规范时也应根据题面、方法名和代码判断。
- 同一道题只维护一个权威题解页面，C# 与 C++ 实现并列展示，不得按语言复制两篇题解。
- 题解页面继续保存在现有 `docs/csharp/leetcode/` 目录以避免大规模链接迁移，但在站点导航和内容职责上视为中立的“双语题解区”。
- C++ 专区只沉淀跨题目复用的语法、容器、指针、标准库和编译错误，不重复题目分析。
- 新题先加入对应题型索引；单题页面不加入 `mkdocs.yml` 全局导航，避免题量增长后导航过长。
- 每次新增题目后同步更新双语题解总数、难度统计和对应题型索引；只有实际提供两种语言实现时才标记为“双语对照”。

## 复盘文件规则

- `docs/reviews/weekly-review.md` 和 `docs/reviews/project-review-template.md` 永远保持为可复制的空白模板，不得直接写入真实复盘。
- 每周复盘保存到 `docs/reviews/YYYY/YYYY-Www.md`，项目复盘使用可识别项目名称的独立文件。
- 新增真实复盘后更新 `docs/reviews/index.md`；历史记录通过索引访问，不逐条加入全局导航。

## 内容与编辑规范

- 文章默认使用简体中文，必要的 API、类型名和行业术语保留英文。
- 优先写清楚“为什么、何时使用、如何验证”，避免只堆砌 API 定义。
- 保留资料来源链接；无法确认来源时不要虚构引用。
- 修改现有文章时保持原有结构和有效内容，只改动与本次资料相关的部分。
- 不编辑生成目录 `site/`，站点内容只能从 `docs/` 和 `mkdocs.yml` 修改。
- 不提交 `inbox/` 和 `archive/` 中的原始资料；这些目录默认是本地工作区。正式文章和整理后的示例可以提交。

## 页面职责

- `docs/roadmap/` 只维护当前阶段、学习顺序、进入条件、下一步和阶段产出。
- 各专题 `index.md` 只列出现有文章、计划主题和查询入口，不重复维护路线状态。
- `docs/checklists/` 只定义掌握标准和验证证据，不承担知识正文。
- 正式知识文章只记录已经理解、练习或验证的内容；只有标题和计划的页面必须列入 `draft_docs`，不能进入生产导航。

## 完成标准

一次 Inbox 整理只有同时满足以下条件才算完成：

- 可识别内容已经合并到正确的权威页面；
- 没有产生明显重复文章；
- 已验证与待验证内容得到明确区分；
- 新增导航和相关链接正确；
- 已执行可用的验证并报告结果；
- 已处理原资料得到安全归档和处理报告；
- 最终回复简要列出变更、验证结果和仍待确认事项。
