# GameDev 知识库跨模型交接协议

> 协议版本：1.0
>
> 生效日期：2026-07-22
>
> 适用对象：参与本仓库整理、维护、审阅、验证或续接任务的所有模型与智能体

## 1. 协议目标

本协议用于让不同模型在不依赖上一段聊天上下文的情况下安全接手知识库，并保证长期维护过程中：

- 原始资料不会丢失或被覆盖；
- 同一知识点只保留一个权威版本；
- 用户实践、外部观点、模型推断和真实验证不会混为一谈；
- 未提交修改、未完成任务和待验证问题能够被下一模型准确识别；
- 每次维护都能增加可查询、可验证、可复盘的知识，而不是单纯增加文档数量。

## 2. 规则优先级

接手模型必须按以下顺序理解约束：

1. 用户在当前任务中的明确要求；
2. 仓库根目录的 `AGENTS.md`；
3. 本文件 `HANDOFF.md`；
4. `docs/guides/note-template.md` 与现有页面结构；
5. 最近的处理报告、聊天摘要或上一模型的说明。

发生冲突时，低优先级内容不能覆盖高优先级规则。处理报告和模型总结只用于定位线索，不能代替实时检查。

## 3. 接手前必须完成的检查

任何会修改仓库的任务开始前，先完成以下只读检查：

```text
1. 阅读 AGENTS.md 和 HANDOFF.md
2. 查看 git status --short --untracked-files=all
3. 扫描 inbox/ 全部内容，包括 Git 忽略文件
4. 查看相关 docs/ 权威页面、题型/专题索引、docs/learning/ 学习区和 mkdocs.yml
5. 必要时查看最近的 archive/YYYY-MM-DD/processing-report*.md
6. 确认当前请求是否真的授权处理 Inbox、归档、提交或其他写操作
```

### 实时状态优先

- 不根据旧聊天直接断言 Inbox 为空、构建通过或某功能已经验证。
- 不根据上一模型的文件列表直接修改；先确认文件仍然存在且内容没有变化。
- 如果处理报告与当前文件冲突，以当前工作区为准，并在本次结果中记录差异。

### 脏工作区保护

- 默认把所有既有修改视为用户或前一模型仍在进行的工作。
- 不使用 `git reset --hard`、`git checkout --` 或其他会丢失修改的命令。
- 不覆盖、删除或格式化与当前任务无关的文件。
- 如果修改与既有变更重叠，先阅读完整差异，再做最小范围编辑。
- 未经用户明确要求，不自动暂存、提交、推送或创建分支。

## 4. 证据与结论分层

所有正式文章、报告和最终回复都必须标明结论来自哪里。

| 等级 | 证据类型 | 可以怎样表述 | 不得怎样表述 |
|---|---|---|---|
| A | 当前模型亲自编译、运行、测试或观察，并保留结果 | “本次运行 5 组用例，全部通过” | 扩大到未覆盖平台、场景或边界 |
| B | 用户实践记录、截图、提交结果，或用户转述其他模型/工具的检查 | “用户记录已运行”“用户转述 Kimi 已检查场景” | “本次已在 Unity 中验证通过” |
| C | 当前模型的静态代码检查、文档推理或可解释推断 | “静态检查未发现语法问题”“从片段可推断……” | “已运行”“已证明零分配” |
| D | 缺少上下文、证据冲突或尚未测试 | “待验证”“需要补充场景/Profiler/提交记录” | 猜测、补写或包装成最佳实践 |

### 证据归属示例

```text
用户说“代码和场景由 Kimi 检查过”
→ 记录为外部检查证据（B）
→ 可以更新“已有外部检查”
→ 不能写成当前模型亲自运行 Unity

模型只拿到一段 Projectile 代码
→ 可以做静态边界分析（C）
→ 不能推断 Prefab、Layer、Collider 和场景引用均正确
```

性能结论必须额外谨慎。API 名称含 `NonAlloc`、使用对象池或没有看到 `new`，都不能单独证明整条业务链“零分配”或“性能更快”；需要 Profiler、基准或明确运行记录。

## 5. 内容映射与去重协议

写入前先回答五个问题：

1. 这份资料属于项目实践、通用知识、路线、检查清单、复盘还是原始证据？
2. 仓库里是否已经有同一主题的权威页面？
3. 新内容是补充、修正、后续演进，还是确实形成独立主题？
4. 哪些内容可以跨项目复用，哪些只能留在项目文章或归档？
5. 如果这是学习证据，它应该追加到哪一周的 `docs/learning/weekly/YYYY-Www.md`，以及是否足以改变 `docs/learning/progress.md` 的能力状态？

### 页面职责

| 位置 | 只承担什么 | 不承担什么 |
|---|---|---|
| `docs/` 正式文章 | 已理解、练习或验证的权威知识 | 原样堆放 Inbox、无证据扩写 |
| 项目文章 | 项目实现、取舍、演进、场景边界 | 冒充通用最佳实践 |
| 专题 `index.md` | 已有文章和查询入口 | 重复维护路线状态和知识正文 |
| `docs/roadmap/` | 当前阶段、顺序、进入条件、下一步 | API 教程和项目正文 |
| `docs/learning/` | 学习进度、按周归集的学习记录、证据索引和下一步安排 | 稳定知识正文、原始 Inbox 堆放、无证据掌握声明 |
| `docs/checklists/` | 掌握标准与验证证据 | 教程正文 |
| `docs/reviews/` | 真实复盘、问题、取舍和下一步 | 覆盖空白模板 |
| `examples/` | 可独立理解、可长期复用的最小示例 | 项目残片、缺依赖代码 |
| `archive/` | 处理后的原始资料和处理报告 | 正式知识的权威版本 |

### 去重规则

- 已有对应页面时优先补充，不以日期、语言或来源再复制一篇。
- 项目同一系统的后续课程优先记录“原方案 → 暴露问题 → 修复方案”的演进链。
- 通用专题只提炼跨项目稳定结论，并链接项目证据。
- LeetCode 同一道题只保留一个页面，C# 与 C++ 并列展示。
- 单题页面通过题型索引访问，不加入全局导航；同步更新总数、难度和双语数量。

## 6. 标准维护流程

### 阶段 1：盘点

- 明确用户请求的范围和授权。
- 扫描输入资料、当前修改和可能被忽略的文件。
- 识别同一主题的文件组、代码/场景依赖和不可读内容。

### 阶段 2：映射

- 搜索现有权威页面和相关索引。
- 决定“更新现有页面、创建新页面、只归档、暂留 Inbox”中的一种。
- 列出资料分别进入项目、专题、路线、检查清单或复盘的理由。

### 阶段 3：提炼

- 保留目标、当前理解、核心思路、最小代码、项目应用、易错点和验证方式。
- 学习证据先追加到对应周学习记录；同一周可以多次整理，每次整理保留批次日期和处理结果。
- 删除无关业务、个人路径、密钥和没有教学价值的重复代码。
- 修正明显错误时说明原问题和修正理由，不悄悄把用户记录改写成另一套结论。
- 对无法确认的内容使用“待验证”，不为了完整度补写。

### 阶段 4：关联

- 更新相关题型/专题索引。
- 更新 `docs/learning/progress.md` 时，必须引用本次整理的周记录或正式知识页作为证据，不能只写主观感觉。
- 只有新增正式页面时才调整 `mkdocs.yml`。
- 为项目证据、通用专题和后续课程添加必要的双向链接。
- 不编辑生成目录 `site/`。

### 阶段 5：验证

至少执行：

```text
mkdocs build --strict
git diff --check
```

并按内容执行适用验证：

- C# / C++：编译、最小运行用例或静态检查；缺编译器时如实说明。
- Unity：代码静态检查、Prefab/场景/`.meta` / GUID 关系、生命周期和配置依赖；无法启动 Unity 时不能声称场景运行通过。
- LeetCode：覆盖普通、空值、边界、极端长度和语言差异；没有 Accepted 证据就保留“判题状态待确认”。
- 性能：记录工具、场景、样本、前后数据和代价；没有数据就只保留设计动机与待测项。

LeetCode 题解有一条例外约定：用户后续主动放入 `inbox/` 的 LeetCode 题解，默认表示用户已在放入前验证通过。接手模型应把它记录为用户实践证据（B），不再为题解代码创建临时工程、编译或运行用例；除非用户明确要求复测，或资料标注未通过、待调试、结果存疑。页面和处理报告应写成“用户放入知识库前已验证通过；本次整理未重复运行代码测试”，不能升级为当前模型亲自运行通过。`python scripts/check_leetcode_stats.py`、`mkdocs build --strict` 和 `git diff --check` 仍需按规则执行。

临时验证文件必须在交接前清理，并确认没有把构建产物留在仓库。

### 阶段 6：归档

只有用户明确要求处理 Inbox 时才归档：

- 原文件移动到 `archive/YYYY-MM-DD/`，不得删除。
- 同名原文件必须增加后缀，禁止覆盖。
- 当天已有 `processing-report.md` 时，使用 `processing-report-2.md`、`processing-report-3.md` 等递增名称。
- 报告记录输入、映射、变更、示例、证据分层、验证结果和待确认事项。
- 学习区整理报告还必须记录：追加/更新了哪一周的学习记录、`progress.md` 哪些状态发生变化、哪些内容只保留为待验证。
- `inbox/` 和 `archive/` 默认不提交 Git。

### 阶段 7：交付

最终回复至少包含：

- 更新或创建的权威页面；
- 更新的学习区文件，包括周记录和学习进度；
- 内容分别归入哪些区域；
- 实际执行的验证及结果；
- 原文件和处理报告的归档位置；
- 仍待确认的问题；
- 是否暂存、提交或推送（未执行时不要暗示已经执行）。

## 7. 未完成任务的交接格式

任务无法在当前模型中完成，或用户准备让另一个模型继续时，必须提供以下交接记录。可以放在最终回复中；如果用户要求持久化，再保存为单独 Markdown 文件。

```markdown
# 任务交接

## 目标

- 本次要达成什么结果。

## 当前状态

- 状态：未开始 / 进行中 / 已完成 / 受阻
- 已完成：
- 尚未完成：

## 输入与主题映射

| 输入 | 识别主题 | 计划写入位置 | 处理状态 |
|---|---|---|---|
|  |  |  |  |

## 已修改文件

- `path/to/file.md`：修改原因与范围

## 证据

| 结论 | 等级 A/B/C/D | 来源或验证方法 |
|---|---|---|
|  |  |  |

## 验证

- 已执行：
- 结果：
- 尚未执行及原因：

## 工作区状态

- 既有用户修改：
- 本任务修改：
- 未跟踪文件：
- 临时文件是否已清理：

## 归档状态

- 已归档：
- 仍在 Inbox：
- 处理报告：

## 风险与下一步

1. 下一模型应先做什么。
2. 哪些结论不能直接沿用。
3. 哪些操作需要用户重新授权。
```

### 受阻条件

以下情况应停止猜测并明确交接：

- 无法判断资料属于哪个项目或题目；
- 缺少关键脚本、场景、Prefab、题号或语言版本，继续写会产生错误结论；
- 文件损坏或当前工具无法读取；
- 需要用户选择会显著改变知识库结构的方案；
- 需要提交、推送、联网、安装依赖或修改外部系统，但当前没有授权。

## 8. 接手提示词模板

用户将任务交给另一个模型时，可以使用：

```text
请先完整阅读仓库根目录的 AGENTS.md 和 HANDOFF.md，再检查实时 git status、inbox/ 和相关权威页面。不要仅依赖上一模型的聊天总结。

任务：<填写任务>

如果是 Inbox 整理：按照 AGENTS.md 完成识别、去重、证据分层、知识库更新、严格构建、归档和 processing report。保留现有未提交修改，不覆盖原始资料。最终列出变更、验证结果、归档位置和待确认事项。

如果是学习记录整理：先追加到对应 `docs/learning/weekly/YYYY-Www.md` 的本次整理批次，再按证据更新 `docs/learning/progress.md`；同一周可多次整理，不需要等到周末。
```

如果上一模型留下了任务交接记录，再追加：

```text
下面是上一模型的交接记录，只作为线索。请以当前文件和实际工具结果复核后继续：
<粘贴交接记录>
```

## 9. 禁止事项

- 不把上一模型的结论直接升级为当前模型的验证结果。
- 不因“另一个模型检查过”而省略证据归属。
- 不处理用户没有要求整理的 Inbox。
- 不删除、覆盖或提交 `inbox/`、`archive/` 原始资料。
- 不把项目专用代码包装成通用最佳实践。
- 不在没有统计现有页面时手工猜题目总数或双语数量。
- 不把 `site/` 当作源文件编辑。
- 不直接向空白周复盘或项目复盘模板写入真实记录。
- 不为追求文章完整而编造版本、来源、运行结果、复杂度或性能数据。
- 不隐藏构建失败、缺少依赖、缺少编译器或未执行测试的事实。

## 10. 完成交接标准

一次跨模型交接只有满足以下条件才算清晰：

- 下一模型能仅凭仓库和交接记录定位目标与权威页面；
- 当前工作区修改的归属和范围明确；
- 所有重要结论都有证据等级和来源；
- 已执行与未执行的验证严格区分；
- Inbox、归档和处理报告状态明确；
- 临时文件已清理；
- 下一步是具体动作，不是“继续完善”之类空泛描述；
- 没有要求下一模型相信未经复核的聊天摘要。

本协议不替代 `AGENTS.md`。它的作用是让不同模型用同一套接手、验证和交付语言维护知识库，从而让每次整理都能安全叠加在已有成果之上。
